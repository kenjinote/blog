---
title: '查看Windows许可证状态的命令'
slug: "Windows のライセンスの状態を確認するコマンド"
date: 2025-04-14T00:41:45+09:00
tags: ["Windows", "许可证", "命令提示符"]
draft: false
image: "img_1.png"
categories: ["PC・数码"]
---

# 【Windows】查看许可证状态的方法（只需一条命令）

您是否曾经想过您的Windows许可证是否已正确激活？

这种情况下，**只需一条命令即可查看许可证信息的方法** 非常方便。只需执行以下步骤，即可轻松检查当前的许可证状态。

## 查看许可证状态的命令

您可以使用Windows内置的脚本工具来显示许可证信息。使用的命令如下：

```
slmgr /dli
```

执行此命令后，将在窗口中显示部分许可证信息。

## 执行方法

1. **在“开始菜单”中输入“cmd”，右键单击“命令提示符” → “以管理员身份运行”**。

2. 在命令提示符中输入以下内容，然后按Enter键：

   ```
   slmgr /dli
   ```

3. 等待几秒钟，将显示如下所示的许可证信息。

   ![Windows许可证确认画面](img.png)

## 显示的主要信息

* 部分产品密钥
* 许可证类型（零售、OEM等）
* 许可证状态（有效、已过期、未激活等）

## 想了解更详细的信息？

还有以下命令：

* `slmgr /dlv`：显示更详细的许可证信息
* `slmgr /xpr`：显示许可证的有效期（是否永久等）

## 总结

只需一条命令即可轻松查看Windows的许可证状态。

* **简单确认**：`slmgr /dli`
* **详细确认**：`slmgr /dlv`
* **确认有效期**：`slmgr /xpr`

如果许可证出现问题，可能会限制更新或某些功能，因此定期检查会更放心。
