import json
import codecs

mapping = {
    "## 4. アルゴリズムの比較とまとめ": "## 4. 算法比较与总结",
    "2. 探索範囲が有効である限り（`low <= high`）、以下の処理を繰り返します。": "2. 只要搜索范围有效（`low <= high`），就重复以下处理。",
    "3. 一致した場合は、その要素のインデックス（位置）を返して探索を終了します。": "3. 如果一致，则返回该元素的索引（位置）并结束搜索。",
    "### 2.2 Pythonによる二分探索の実装（反復法）": "### 2.2 使用 Python 实现二分搜索（迭代法）",
    "    B --> C{\"i < 配列の長さ?\"}": "    B --> C{\"i < 数组长度?\"}",
    "print(f\"grapeの価格: {ht.search('grape')}円\")  # 存在しないキー": "print(f\"grape的价格: {ht.search('grape')}日元\")  # 不存在的键",
    "    C -- \"No\" --> D[\"探索失敗\"]": "    C -- \"No\" --> D[\"搜索失败\"]",
    "検索（Search）も同様に、検索したいキーのハッシュ値を計算し、インデックスを求めてその場所のデータを確認するだけです。キーから保存場所が直接計算できるため、データの量に関わらず瞬時に検索が完了します（ $O(1)$ の時間計算量）。": "搜索（Search）也一样，只需计算要搜索的键的哈希值，求出索引并确认该位置的数据即可。由于可以直接从键计算出保存位置，因此无论数据量多少，都可以瞬间完成搜索（时间复杂度为 $O(1)$ ）。",
    "ハッシュテーブルの設計において、ハッシュ関数の品質と配列のサイズ（負荷率：Load Factor）の管理は極めて重要です。データ要素の数が配列のサイズに対して多くなりすぎる（負荷率が高くなる）と、ハッシュ衝突が頻発し、チェイン法では連結リストが長くなり、オープンアドレス法では空き場所を探すための探査回数が増加します。結果として、探索時間が $O(1)$ から $O(n)$ へと劣化してしまいます。これを防ぐために、多くのハッシュテーブルの実装（Pythonの組み込み辞書 `dict` など）では、要素数が増えると自動的に配列のサイズを拡張し、すべての要素のハッシュ値を再計算して配置し直す「リハッシュ（Rehashing）」という処理が行われます。": "在哈希表的设计中，哈希函数的质量和数组大小（负载因子：Load Factor）的管理极其重要。如果数据元素的数量相对于数组大小过多（负载因子变高），哈希冲突就会频繁发生，在链地址法中链表会变长，在开放地址法中寻找空位的探测次数会增加。结果会导致搜索时间从 $O(1)$ 退化到 $O(n)$ 。为了防止这种情况，在许多哈希表的实现中（例如 Python 的内置字典 `dict`），当元素数量增加时，会自动扩展数组的大小，并重新计算所有元素的哈希值进行重新排列，这个过程称为“重哈希（Rehashing）”。",
    "    print(f\"要素 {target_value} はリスト内に存在しません。\")": "    print(f\"元素 {target_value} 不存在于列表中。\")",
    "description: \"データの中から目的の値を見つけ出す探索アルゴリズム。線形探索、二分探索の基礎から、高速な検索を実現するハッシュテーブルの原理まで解説します。\"": "description: \"从数据中找出目标值的搜索算法。从线性搜索、二分搜索的基础，到实现高速搜索的哈希表原理进行详细解说。\"",
    "7. 中央の要素がターゲットより大きい場合は、ターゲットは左半分の範囲に存在することになるため、右端を `mid - 1` に更新します。": "7. 如果中间的元素大于目标值，说明目标值存在于左半部分的范围内，因此将右端更新为 `mid - 1`。",
    "    :param target: 見つけ出したい値": "    :param target: 想要查找的值",
    "2. 計算されたハッシュ値を、ハッシュテーブルの配列のサイズで割った余り（モジュロ演算）を求め、実際のインデックスを決定します。": "2. 求出计算得到的哈希值除以哈希表数组大小的余数（模运算），以确定实际的索引。",
    "    F -- \"Yes\" --> G[\"mid を返す\"]": "    F -- \"Yes\" --> G[\"返回 mid\"]",
    "## 1. 線形探索（Linear Search）": "## 1. 线性搜索（Linear Search）",
    "4. 中央の要素（`arr[mid]`）と目的の値（ターゲット）を比較します。": "4. 比较中间的元素（`arr[mid]`）和目标值。",
    "    二分探索（反復法）を実行する関数": "    执行二分搜索（迭代法）的函数",
    "## 3. ハッシュテーブル（Hash Table）の原理と構造": "## 3. 哈希表（Hash Table）的原理与结构",
    "本記事では、コンピュータサイエンスの基礎である「線形探索（Linear Search）」と「二分探索（Binary Search）」のアルゴリズムについて、その仕組みや計算量、Pythonによる実装例を交えて詳細に解説します。さらに、これらのアルゴリズムの限界を突破し、圧倒的な検索速度を実現する「ハッシュテーブル（Hash Table）」の原理、ハッシュ関数の役割、そしてハッシュ衝突（Collision）の解決手法に至るまで、深く掘り下げていきます。": "本文将结合其机制、时间复杂度和 Python 实现示例，详细解说计算机科学的基础——“线性搜索（Linear Search）”和“二分搜索（Binary Search）”算法。此外，还将突破这些算法的局限，深入探讨实现压倒性搜索速度的“哈希表（Hash Table）”的原理、哈希函数的作用，以及哈希冲突（Collision）的解决方法。",
    "    A[\"探索開始\"] --> B[\"インデックス i = 0\"]": "    A[\"搜索开始\"] --> B[\"索引 i = 0\"]",
    "4. 一致しなかった場合は、次の要素に進みます。": "4. 如果不一致，则进入下一个元素。",
    "探索アルゴリズムの理解を深めることは、プログラマーとしてのスキルを一段階引き上げるために不可欠です。データ量が少ない場合はアルゴリズムの選択がパフォーマンスに与える影響は微小かもしれませんが、ビッグデータの時代において、数百万、数億というデータの中から瞬時に目的の情報を探し出すためには、適切なアルゴリズムとデータ構造の選択が極めて重要になります。特に、 **時間計算量** ($O(n)$ や $O(\\log n)$、$O(1)$ など) の概念を理解することは、効率的なプログラムを設計する上で欠かせない要素です。": "深入理解搜索算法，对于将程序员的技能提升一个台阶来说是不可或缺的。如果数据量较少，算法选择对性能的影响可能微乎其微，但在大数据时代，为了能从数百万、数亿的数据中瞬间找出目标信息，选择合适的算法和数据结构变得极其重要。特别是理解 **时间复杂度** （如 $O(n)$ 、 $O(\\log n)$ 、 $O(1)$ 等）的概念，在设计高效程序时是必不可少的要素。",
    "print(f\"bananaの価格: {ht.search('banana')}円\")": "print(f\"banana的价格: {ht.search('banana')}日元\")",
    "線形探索の最大の特徴は、データがソート（整列）されている必要がないという点です。データがバラバラの順序で格納されていても、先頭から順番に確認していくため、確実に目的の値を見つけ出すことができます（または存在しないことを確認できます）。しかし、この「順番にすべてを確認する」という性質が、データ量が多くなった場合のパフォーマンス低下の最大の要因となります。": "线性搜索的最大特点在于不需要对数据进行排序。即使数据以杂乱无章的顺序存储，由于是从头开始按顺序确认，也能确实地找到目标值（或者确认其不存在）。然而，这种“按顺序确认所有内容”的性质，正是数据量变大时性能下降的最大原因。",
    "二分探索の驚異的な性能は、探索範囲を毎回半分に分割するという性質から来ています。例えば、要素数が100万個ある配列に対して線形探索を行うと、最悪の場合100万回の比較が必要になりますが、二分探索を用いれば、わずか20回程度の比較で目的の値を見つけることができます（$2^{20} \\approx 1,000,000$）。このため、大規模なデータセットに対する検索操作において、二分探索は線形探索と比較して圧倒的な優位性を誇ります。数学的には、二分探索の時間計算量は $O(\\log n)$ と表現されます。": "二分搜索惊人的性能来自于每次将搜索范围对半分割的性质。例如，对含有 100 万个元素的数组进行线性搜索，最坏的情况下需要进行 100 万次比较，但如果使用二分搜索，只需大约 20 次比较就能找到目标值（$2^{20} \\approx 1,000,000$）。因此，在针对大规模数据集的搜索操作中，二分搜索与线性搜索相比拥有压倒性的优势。在数学上，二分搜索的时间复杂度表示为 $O(\\log n)$ 。",
    "    A[\"探索開始\"] --> B[\"low = 0, high = len - 1\"]": "    A[\"搜索开始\"] --> B[\"low = 0, high = len - 1\"]",
    "### 3.3 ハッシュ衝突（Collision）とその解決法": "### 3.3 哈希冲突（Collision）及其解决方法",
    "ここまで解説してきた3つの探索アルゴリズム（線形探索、二分探索、ハッシュテーブル）の特性を比較表にまとめます。": "将目前为止解说过的 3 种搜索算法（线性搜索、二分搜索、哈希表）的特性总结在比较表中。",
    "| アルゴリズム | 時間計算量 (平均) | 時間計算量 (最悪) | 空間計算量 | 前提条件 | 特徴 |": "| 算法 | 时间复杂度 (平均) | 时间复杂度 (最坏) | 空间复杂度 | 前提条件 | 特点 |",
    "| **ハッシュテーブル** | $O(1)$ | $O(n)$ | $O(n)$ | ハッシュ関数が必要 | 圧倒的な高速検索が可能だが、メモリを多く消費し、最悪ケースでの性能劣化に注意。 |": "| **哈希表** | $O(1)$ | $O(n)$ | $O(n)$ | 需要哈希函数 | 能够进行压倒性的高速搜索，但内存消耗大，需注意最坏情况下的性能退化。 |",
    "    C --> D[\"配列のインデックス 5 に格納\"]": "    C --> D[\"存储到数组的索引 5\"]",
    "3. 決定したインデックスの場所に、キーと値のペアを保存します。": "3. 在决定的索引位置，保存键值对。",
    "| **線形探索** | $O(n)$ | $O(n)$ | $O(1)$ | なし | 実装が簡単。小規模データや未ソートデータに適用。 |": "| **线性搜索** | $O(n)$ | $O(n)$ | $O(1)$ | 无 | 实现简单。适用于小规模数据或未排序数据。 |",
    "チェイン法は、配列の各インデックスに「連結リスト（Linked List）」を持たせる手法です。ハッシュ衝突が発生した場合、同じインデックスの連結リストに新しい要素を追加していきます。": "链地址法是在数组的每个索引处配置一个“链表（Linked List）”的手法。当发生哈希冲突时，将新元素追加到同一索引的链表中。",
    "### 3.1 ハッシュ関数の役割": "### 3.1 哈希函数的作用",
    "    B --> C[\"ハッシュ値 (例: 5)\"]": "    B --> C[\"哈希值 (例: 5)\"]",
    "* **平方探査（Quadratic Probing）** : 衝突が起きたインデックスから、1の2乗、2の2乗、3の2乗... と間隔を広げながら空き場所を探します。": "* **平方探测（Quadratic Probing）** : 从发生冲突的索引开始，按照 1 的平方、2 的平方、3 的平方……这样逐渐扩大间隔来寻找空位。",
    "6. 中央の要素がターゲットより小さい場合は、ターゲットは右半分の範囲に存在することになるため、左端を `mid + 1` に更新します。": "6. 如果中间的元素小于目标值，说明目标值存在于右半部分的范围内，因此将左端更新为 `mid + 1`。",
    "# テストデータ": "# 测试数据",
    "    D -- \"Yes\" --> F[\"インデックス i を返す\"]": "    D -- \"Yes\" --> F[\"返回索引 i\"]",
    "1. 配列の最初の要素を取り出します。": "1. 取出数组的第一个元素。",
    "8. 探索範囲がなくなっても見つからなかった場合は、探索失敗とします。": "8. 如果搜索范围消失仍未找到，则视为搜索失败。",
    "    C -- \"No\" --> E[\"探索失敗（見つからず）\"]": "    C -- \"No\" --> E[\"搜索失败（未找到）\"]",
    "title: \"探索アルゴリズム（線形、二分探索とハッシュテーブルの原理）\"": "title: \"搜索算法（线性、二分搜索与哈希表原理）\"",
    "#### 3.3.2 オープンアドレス法（Open Addressing）": "#### 3.3.2 开放地址法（Open Addressing）",
    "# ハッシュテーブルのテスト": "# 哈希表测试",
    "二分探索は以下の手順で行われます。": "二分搜索按以下步骤进行。",
    "    print(f\"要素 {target_value} はインデックス {result_index} で見つかりました。\")": "    print(f\"元素 {target_value} 在索引 {result_index} 处被找到。\")",
    "### 3.2 ハッシュテーブルへのデータの追加と検索": "### 3.2 向哈希表添加数据与搜索",
    "ハッシュ関数の出力範囲（配列のサイズ）には限りがあるため、異なるキーから同じハッシュ値（同じインデックス）が生成されてしまうことがあります。これを **ハッシュ衝突（Collision）** と呼びます。ハッシュ衝突は避けられない問題であるため、それを解決するための適切な手法が必要になります。": "由于哈希函数的输出范围（数组的大小）是有限的，不同的键可能会生成相同的哈希值（相同的索引）。这被称为 **哈希冲突（Collision）** 。因为哈希冲突是不可避免的问题，所以需要适当的手法来解决它。",
    "                    current.value = value  # キーが既に存在する場合は値を更新": "                    current.value = value  # 如果键已经存在，则更新值",
    "### 2.1 二分探索の仕組み": "### 2.1 二分搜索的机制",
    "線形探索の $O(n)$ 、二分探索の $O(\\log n)$ に対して、さらに高速な $O(1)$ （定数時間）での探索を目指すデータ構造が **ハッシュテーブル** （またはハッシュマップ）です。ハッシュテーブルは、「キー（Key）」と「値（Value）」のペアを保存し、キーを用いて値を瞬時に取り出すことができる強力な仕組みです。": "相对于线性搜索的 $O(n)$ 和二分搜索的 $O(\\log n)$ ，旨在实现更高速的 $O(1)$ （常数时间）搜索的数据结构就是 **哈希表** （或哈希映射）。哈希表保存“键（Key）”和“值（Value）”的键值对，是一种能够使用键瞬间取出值的强大机制。",
    "    :param arr: 探索対象のリスト": "    :param arr: 搜索目标的列表",
    "代表的な空き場所の探し方（探査手法）には以下のようなものがあります。": "代表性的寻找空位的方法（探测手法）有以下几种。",
    "以下に、Pythonを用いた線形探索のシンプルな実装例を示します。": "下面展示使用 Python 实现线性搜索的简单示例。",
    "### 1.1 線形探索の仕組み": "### 1.1 线性搜索的机制",
    "1. **計算が高速であること** : キーからハッシュ値を求める処理に時間がかかっては、検索全体のパフォーマンスが低下します。": "1. **计算要快** : 如果从键计算哈希值的过程耗时，整体搜索的性能就会下降。",
    "5. 配列の最後まで確認し、ターゲットが見つからなかった場合は、探索失敗（例えば `-1` や `None` を返す）として終了します。": "5. 确认到数组的末尾，如果没有找到目标值，则作为搜索失败（例如返回 `-1` 或 `None`）结束。",
    "理想的なハッシュ関数は、以下の条件を満たす必要があります。": "理想的哈希函数需要满足以下条件。",
    "5. 一致した場合は、`mid` を返して終了します。": "5. 如果一致，则返回 `mid` 并结束。",
    "線形探索は、データ構造（配列やリストなど）の先頭から末尾に向かって、目的の値が見つかるまで順番に一つずつ要素を確認していく、最もシンプルで直感的な探索アルゴリズムです。": "线性搜索是从数据结构（数组或列表等）的开头向末尾，按顺序逐个确认元素，直到找到目标值为止的，最简单且直观的搜索算法。",
    "    線形探索を実行する関数": "    执行线性搜索的函数",
    "print(f\"appleの価格: {ht.search('apple')}円\")": "print(f\"apple的价格: {ht.search('apple')}日元\")",
    "3. **分布が一様であること** : 異なるキーを入力した際に、ハッシュ値が配列の様々なインデックスに均等に分散される（偏りがない）ことが求められます。": "3. **分布要均匀** : 要求在输入不同的键时，哈希值能够均匀地分散到数组的各个索引中（不产生偏移）。",
    "ハッシュテーブルへのデータの追加（Insert）は以下の手順で行われます。": "向哈希表添加数据（Insert）按以下步骤进行。",
    "#### 3.3.1 チェイン法（Separate Chaining）": "#### 3.3.1 链地址法（Separate Chaining）",
    "現代のコンピュータサイエンスにおいて、データの中から目的の値を迅速に見つけ出す **探索アルゴリズム** は、あらゆるソフトウェアやシステムの根幹をなす非常に重要な技術です。データベースの検索、ウェブブラウザでのキーワード検索、スマートフォンの連絡先アプリでの名前の検索など、私たちは日常的に探索アルゴリズムの恩恵を受けています。": "在现代计算机科学中，迅速从数据中找出目标值的 **搜索算法** 是构成各种软件和系统根基的极其重要的技术。数据库的搜索、网络浏览器中的关键字搜索、智能手机联系人应用中的名字搜索等，我们在日常生活中经常受惠于搜索算法。",
    "# 探索アルゴリズムの基礎と重要性": "# 搜索算法的基础与重要性",
    "1. 追加したいデータのキーをハッシュ関数に渡し、ハッシュ値を計算します。": "1. 将想要添加的数据的键传递给哈希函数，计算哈希值。",
    "線形探索のアルゴリズムは以下の手順で進行します。": "线性搜索算法按以下步骤进行。",
    "### 1.2 Pythonによる線形探索の実装": "### 1.2 使用 Python 实现线性搜索",
    "# テストデータ（ソート済みである必要がある）": "# 测试数据（必须已排序）",
    "以下に、Pythonを用いてチェイン法によるハッシュ衝突解決を備えたシンプルなハッシュテーブルを実装します。": "下面展示使用 Python 实现具备链地址法哈希冲突解决的简单哈希表。",
    "| **二分探索** | $O(\\log n)$ | $O(\\log n)$ | $O(1)$ | ソート済みであること | 高速。配列などランダムアクセス可能なデータ構造が必要。 |": "| **二分搜索** | $O(\\log n)$ | $O(\\log n)$ | $O(1)$ | 必须已排序 | 高速。需要数组等支持随机访问的数据结构。 |",
    "2. 取り出した要素が目的の値（ターゲット）と一致するかどうかを確認します。": "2. 确认取出的元素是否与目标值相匹配。",
    "1. 探索対象の配列の「左端（`low`）」と「右端（`high`）」のインデックスを初期化します。": "1. 初始化搜索目标数组的“左端（`low`）”和“右端（`high`）”索引。",
    "        return None  # キーが見つからない場合": "        return None  # 如果没有找到键",
    "ハッシュテーブルの中核を担うのが **ハッシュ関数** です。ハッシュ関数は、任意のデータ（キー）を入力として受け取り、固定長の整数値（ハッシュ値）を出力する関数です。このハッシュ値を用いて、データを配列（バケット）のどのインデックスに保存するかを決定します。": "担纲哈希表核心的是 **哈希函数** 。哈希函数接收任意数据（键）作为输入，输出固定长度的整数值（哈希值）。通过使用这个哈希值，决定将数据保存到数组（桶）的哪一个索引中。",
    "    A[\"キー (例: 'Apple')\"] --> B[\"ハッシュ関数\"]": "    A[\"键 (例: 'Apple')\"] --> B[\"哈希函数\"]",
    "    D -- \"No\" --> G[\"i を 1 増やす\"]": "    D -- \"No\" --> G[\"i 增加 1\"]",
    "    C -- \"Yes\" --> D{\"配列[i] == ターゲット?\"}": "    C -- \"Yes\" --> D{\"数组[i] == 目标值?\"}",
    "    :return: 見つかった場合はそのインデックス、見つからない場合は -1": "    :return: 如果找到则返回其索引，如果未找到则返回 -1",
    "* **ダブルハッシュ（Double Hashing）** : 2つ目の異なるハッシュ関数を用いて、次の空き場所を探す間隔を決定します。": "* **双重哈希（Double Hashing）** : 使用第二个不同的哈希函数来决定寻找下一个空位的间隔。",
    "    :param arr: ソート済みの探索対象リスト": "    :param arr: 已排序的搜索目标列表",
    "## 2. 二分探索（Binary Search）": "## 2. 二分搜索（Binary Search）",
    "### 3.4 Pythonによるハッシュテーブルの実装（チェイン法）": "### 3.4 使用 Python 实现哈希表（链地址法）",
    "オープンアドレス法は、追加のデータ構造（連結リストなど）を用いず、ハッシュテーブルの配列自体にすべてのデータを格納する手法です。衝突が発生した場合、あらかじめ決められた規則に従って「空いている別のインデックス（バケット）」を探し、そこにデータを格納します。": "开放地址法是不使用追加的数据结构（如链表等），而是将所有数据直接存储在哈希表数组本身的手法。当发生冲突时，按照预先定好的规则寻找“空着的另一个索引（桶）”，并将数据存储在里面。",
    "3. 探索範囲の中央のインデックス（`mid`）を計算します。": "3. 计算搜索范围中间的索引（`mid`）。",
    "状況に応じて適切なアルゴリズムを選択することが、システムのパフォーマンス最適化の鍵となります。メモリに余裕があり、検索速度を最優先する場合はハッシュテーブルが最適です。メモリ制約があり、データが常にソートされた状態を保てるのであれば二分探索が強力な選択肢となります。データ数が非常に少ない場合や、データの追加・削除が頻繁でソートを維持するコストが高い場合は、シンプルな線形探索が結果的に最良の選択となることもあります。": "根据情况选择合适的算法是系统性能优化的关键。如果内存充裕，且将搜索速度放在首位，则哈希表是最佳选择。如果有内存限制，并且能保证数据始终保持已排序状态，那么二分搜索将是一个强有力的选择。如果数据量极少，或者由于数据的添加/删除频繁导致维护排序的成本很高，那么简单的线性搜索最终可能会成为最佳选择。",
    "二分探索は、**あらかじめソート（昇順または降順に整列）されたデータ** に対してのみ適用できる、非常に高速で効率的な探索アルゴリズムです。探索範囲を半分ずつ絞り込んでいくことで、計算量を劇的に削減します。": "二分搜索是仅能应用于 **预先已排序（升序或降序排列）的数据** 的极其高速且高效的搜索算法。通过将搜索范围逐次对半缩小，可以剧烈地减少计算量。",
    "* **線形探査（Linear Probing）** : 衝突が起きたインデックスから順番に（+1, +2, ...）、次の空き場所を探します。": "* **线性探测（Linear Probing）** : 从发生冲突的索引开始，按顺序（+1, +2, ...）寻找下一个空位。",
    "2. **決定論的であること** : 同じキーを入力すれば、必ず同じハッシュ値が出力されなければなりません。": "2. **要具有确定性** : 如果输入相同的键，则必须输出相同的哈希值。",
    "categories: [\"computer-science\"]": "categories: [\"计算机科学\"]",
    "tags: [\"algorithms\", \"search\", \"binary-search\", \"hash-table\", \"data-structures\"]": "tags: [\"算法\", \"搜索\", \"二分搜索\", \"哈希表\", \"数据结构\"]"
}

with codecs.open(r'c:\work\kenji.blog\content\post\search-algorithms-linear-binary-hash-table-principles\index.md', 'r', 'utf-8') as f:
    lines = f.readlines()

out_lines = []
for line in lines:
    stripped_line = line.rstrip('\r\n')
    if stripped_line in mapping:
        out_lines.append(mapping[stripped_line] + '\n')
    else:
        out_lines.append(line)

with codecs.open(r'c:\work\kenji.blog\content\post\search-algorithms-linear-binary-hash-table-principles\index.zh-cn.md', 'w', 'utf-8') as f:
    f.writelines(out_lines)

print("Translation to Chinese completed!")
