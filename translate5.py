import re
content = open('c:/work/kenji.blog/content/post/sorting-algorithms-visualized-bubble-quick-merge/index.zh-tw.md', 'r', encoding='utf-8').read()

content = content.replace('クイックソートの詳細トレース', '快速排序的詳細追蹤')
content = content.replace('要素数50のランダムな配列に対してクイックソートを実行した際の、各ステップ（パーティション分割）完了後の状態を示します。ピボット（基準値）によって配列がどのように大小のグループに分かれていくかを確認してください。', '顯示針對具有 50 個元素的隨機陣列執行快速排序時，每個步驟（分割）完成後的狀態。請確認陣列是如何根據樞紐（基準值）被劃分為大小群組的。')
content = re.sub(r'\*\*ステップ (\d+) \(要素 (\d+) 個\)\*\*', r'**步驟 \1 (元素 \2 個)**', content)

content = content.replace('マージソートの詳細トレース', '合併排序的詳細追蹤')
content = content.replace('要素数50のランダムな配列に対してマージソートを実行した際の、各マージ操作完了後の配列の状態を示します。細かく分割された配列が、ソート順を保ちながら徐々に大きな配列へと結合（マージ）されていく過程がわかります。', '顯示針對具有 50 個元素的隨機陣列執行合併排序時，每次合併操作完成後的陣列狀態。可以了解細分的陣列在保持排序順序的情況下，逐漸結合（合併）成較大陣列的過程。')
content = re.sub(r'\*\*ステップ (\d+) \(要素 (\d+) 統合後\)\*\*', r'**步驟 \1 (合併元素 \2 後)**', content)

# specific text from 125
content = content.replace('分割統治法を用いた非常に高速なアルゴリズムです。配列の中から基準値（ピボット）を選び、ピボットより小さい要素と大きい要素に分割します。この操作を再帰的に繰り返すことで全体をソートします。', '這是一種使用分治法的非常快速的演算法。從陣列中選擇基準值（樞紐），並分割成比樞紐小與大的元素。透過遞迴重複此操作來排序整體。')
content = content.replace('C --> E{"要素数が1以下か"}', 'C --> E{"元素數量是否小於等於1"}')
content = content.replace('D --> F{"要素数が1以下か"}', 'D --> F{"元素數量是否小於等於1"}')
content = content.replace('クイックソートは、実務において最も頻繁に利用されるアルゴリズムの一つです。最悪計算量は $\\text{O}(n^2)$ ですが、適切なピボット選択戦略（例えばMedian-of-Three）を用いることで、実質的には常に $\\text{O}(n \\log n)$ で動作します。Pythonの組み込みソート関数 `list.sort()` は、クイックソートではなく、マージソートと挿入ソートを組み合わせた **TimSort (ティムソート)** と呼ばれるアルゴリズムを採用しています。', '快速排序是實務中最頻繁使用的演算法之一。雖然最差複雜度為 $\\text{O}(n^2)$，但透過使用適當的樞紐選擇策略（例如 Median-of-Three），實質上總能以 $\\text{O}(n \\log n)$ 運作。Python 內建的排序函式 `list.sort()` 並非使用快速排序，而是採用了結合合併排序與插入排序、稱為 **TimSort** 的演算法。')
content = content.replace('クイックソートのパフォーマンスは、ピボットの選び方に大きく依存します。理想的には、常に配列の中央値（メジアン）をピボットとして選択できれば、配列は毎回正確に半分に分割され、再帰の深さは $\\text{O}(\\log n)$ となり、完璧な $\\text{O}(n \\log n)$ の計算量が保証されます。しかし、真の中央値を厳密に見つけ出すには追加の計算コストがかかるため、実用上は定数時間で選択できる近似手法が採用されます。', '快速排序的效能很大程度上取決於樞紐的選擇方式。理想情況下，若總能選擇陣列的中位數（Median）作為樞紐，陣列每次就能精確地分成兩半，遞迴深度將為 $\\text{O}(\\log n)$，並保證完美的 $\\text{O}(n \\log n)$ 複雜度。然而，嚴密尋找真正的中位數需要額外的計算成本，因此實務上會採用能在常數時間內選擇的近似手法。')
content = content.replace('もし配列がすでにソートされている状態で、常に先頭の要素をピボットとして選択した場合、分割された配列の片方は要素数が0、もう片方は $n-1$ となり、再帰の深さが $n$ に達してしまいます。これにより、時間計算量は最悪の $\\text{O}(n^2)$ となり、場合によってはスタックオーバーフローを引き起こす危険性もあります。', '如果陣列在已排序的狀態下，總是選擇開頭的元素作為樞紐，分割出來的陣列其中一邊元素數為 0，另一邊為 $n-1$，遞迴深度將達到 $n$。這會導致時間複雜度變成最差的 $\\text{O}(n^2)$，有時甚至有引發堆疊溢位 (Stack Overflow) 的危險。')
content = content.replace('このような事態を防ぐため、ランダムにピボットを選択するランダム化クイックソートや、配列の先頭・中央・末尾の3つの要素の中央値をピボットとして採用する手法が一般的に用いられます。これにより、どのような入力データに対しても安定して高速なソート処理を実現できるようになります。', '為了防止這種事態，通常會使用隨機選擇樞紐的隨機化快速排序，或是採用陣列開頭、中間、末端 3 個元素的中位數作為樞紐的手法。如此一來，對於任何輸入資料都能實現穩定且高速的排序處理。')
content = content.replace('ジョン・フォン・ノイマンによって考案された、安定な外部ソートの代表格です。分割統治法に基づき、配列を細かく分割してから、それらをソートされた順序を保ちながら結合（マージ）していきます。', '由約翰·馮·紐曼 (John von Neumann) 所發明，是穩定的外部排序之代表。基於分治法，將陣列細分後，在保持已排序順序的情況下將它們結合（合併）。')
content = content.replace('A["配列を中央で2つに分割"] --> B{"要素数が1以下か"}', 'A["將陣列從中央分為兩個"] --> B{"元素數量是否小於等於1"}')
content = content.replace('C --> D["隣り合う部分配列をマージ"]', 'C --> D["合併相鄰的部分陣列"]')
content = content.replace('マージソートの最大の特徴は、その **安定性** にあります。安定なソートとは、同じ値を持つ要素の相対的な順序がソート前後で変わらないことを意味します。この性質は、複数の異なる基準でデータを連続してソートする場合に極めて重要になります。', '合併排序最大的特徵在於其 **穩定性**。穩定的排序意味著具有相同值的元素之相對順序在排序前後不會改變。這項性質在以多個不同的基準連續對資料進行排序時極為重要。')
content = content.replace('たとえば、生徒のデータを「テストの点数」でソートした後、「クラス名」でソートし直す場合を考えます。安定なソートアルゴリズムを使用すれば、同じクラスの生徒同士は「テストの点数」の順序が保たれたままになります。マージソートは、結合処理（マージ）において等しい要素の順序を厳密に管理するため、この安定性を完全に保証します。', '例如，考慮在對學生資料「考試分數」進行排序後，再以「班級名稱」重新排序的情況。如果使用穩定的排序演算法，相同班級的學生之間「考試分數」的順序將會被保留。合併排序在結合處理（合併）中會嚴密管理相等元素的順序，因此能完全保證這項穩定性。')
content = content.replace('また、マージソートはデータへのアクセスが逐次的であるため、配列をメモリ上にすべて展開できないような巨大なデータセット（外部メモリ）をソートする際にも極めて有効です。ディスクI/Oの回数を最小限に抑えつつ、効率的にソート処理を進めることができます。これにより、データベースシステムや巨大ファイルの整列処理において、マージソートは今なお最前線で活用され続けています。', '此外，由於合併排序對資料的存取是循序的，所以在對無法將陣列完全展開於記憶體上的巨大資料集（外部記憶體）進行排序時也極為有效。能將磁碟 I/O 次數降至最低，並有效率地推進排序處理。因此，在資料庫系統或巨大檔案的排列處理中，合併排序至今仍持續在最前線被活用。')
content = content.replace('さまざまなソートアルゴリズムを紹介してきましたが、実務において「唯一の正解」となるアルゴリズムは存在しません。データの性質や制約条件に応じて、適切なアルゴリズムを選択することが求められます。', '雖然介紹了各種排序演算法，但在實務中並不存在「唯一正解」的演算法。需要根據資料的性質與限制條件，選擇適當的演算法。')
content = content.replace('- 一般的な用途で最高速を求める場合は **クイックソート** が最適です。', '- 在一般用途尋求最高速時，**快速排序** 是最佳選擇。')
content = content.replace('- メモリ制約が厳しく、インプレースで安定した性能を出したい場合は **ヒープソート** が適しています。', '- 在記憶體限制嚴格，且希望在原地發揮穩定效能的情況，適合使用 **堆積排序**。')
content = content.replace('近代的なプログラミング言語（Python, Java, Rustなど）の標準ライブラリでは、これらのアルゴリズムの長所を組み合わせたハイブリッド手法（TimSortやIntroSortなど）が採用されており、開発者が自分でソートアルゴリズムをゼロから実装する機会は減っています。しかし、その内部でどのようなトレードオフが考慮されているかを理解することは、よりパフォーマンスの高い堅牢なソフトウェアを設計するための重要な基盤となります。', '近代程式語言（Python, Java, Rust 等）的標準函式庫中，採用了結合這些演算法長處的混合手法（如 TimSort 或 IntroSort 等），開發者自己從零開始實作排序演算法的機會已經減少。然而，理解其內部考慮了哪些權衡，將是設計更高效能且穩固軟體的重要基礎。')
content = content.replace('この記事が、あなたのアルゴリズム学習と実務開発の助けになることを願っています。', '希望這篇文章能對您的演算法學習與實務開發有所幫助。')
content = content.replace('A["将阵列从中央分为两个"] --> B{"元素数量是否小于等于1"}', 'A["將陣列從中央分為兩個"] --> B{"元素數量是否小於等於1"}')

with open('c:/work/kenji.blog/content/post/sorting-algorithms-visualized-bubble-quick-merge/index.zh-tw.md', 'w', encoding='utf-8') as f:
    f.write(content)
